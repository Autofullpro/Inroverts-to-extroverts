# Backend

Simple Express backend to proxy chat requests to Gemini/HuggingFace and provide sample users data.

Run locally:

1. Install dependencies
   npm install

2. Copy example env
   cp .env.example .env
   // fill keys locally

3. Start server
   npm run dev
